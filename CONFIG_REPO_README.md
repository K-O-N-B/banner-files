# banner-files

🔔 **Banner配置仓库** - 用于 NZ-Workspace-Assistant 的动态Banner通知系统

## 📋 说明

此仓库仅包含公开的Banner配置文件，不包含任何敏感代码。主项目代码保持私有。

## 📝 配置文件

### `banner-config.json`

```json
{
  "version": "v1.0.0",
  "icon": "🧪",
  "message": "【测试模式】这是从GitHub自动加载的Banner配置！修改version字段可触发所有用户强制更新。",
  "enabled": true,
  "updated_at": "2025-10-10T12:00:00Z"
}
```

## 🔄 如何更新Banner

1. **编辑此仓库中的 `banner-config.json` 文件**
2. **务必修改 `version` 字段**（否则用户不会收到更新）
3. **提交更改**
4. **等待最多30秒** - 所有用户自动同步更新

## 🎯 配置字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `version` | String | ✅ | 版本号，修改后触发强制更新 |
| `icon` | String | ✅ | Banner图标（emoji） |
| `message` | String | ✅ | Banner显示的消息 |
| `enabled` | Boolean | ✅ | 是否启用Banner |
| `updated_at` | String | ❌ | 更新时间（ISO8601格式） |

## ⚙️ 功能特性

- ✅ **自动更新检测**（30秒轮询）
- ✅ **版本控制强制刷新**
- ✅ **离线缓存降级**
- ✅ **NEW标签提示**
- ✅ **智能轮询优化**

## 🔗 相关链接

- **主项目**：NZ-Workspace-Assistant (Private)
- **配置文件URL**：[banner-config.json](https://raw.githubusercontent.com/K-O-N-B/banner-files/main/banner-config.json)

## 📄 License

MIT License - 与主项目保持一致

---

> **注意**：此仓库由 NZ-Workspace-Assistant 自动读取，请勿删除或重命名 `banner-config.json` 文件。

